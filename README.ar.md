# FM Dashboard - دليل التثبيت السريع

## 🚀 تثبيت سريع (أمر واحد فقط!)

```bash
bash install.sh
```

السكريبت سيقوم بـ:
- ✅ فحص المتطلبات تلقائياً
- ✅ اكتشاف مسارات FM stacks تلقائياً
- ✅ توليد رموز أمان عشوائية
- ✅ إنشاء ملف التكوين
- ✅ تثبيت كل المتطلبات
- ✅ اختبار الخدمات
- ✅ إنشاء سكريبتات التشغيل

**فقط سيسألك عن:**
- اسم المستخدم
- كلمة المرور

## 📦 التثبيت خطوة بخطوة

### 1. تحميل المشروع

```bash
cd /opt
sudo chown $USER:$USER /opt
git clone https://github.com/Baron-Systems/dash.git
cd dash
```

### 2. تشغيل سكريبت التثبيت

```bash
bash install.sh
```

### 3. بدء الخدمات

**خيار 1: تشغيل جميع الخدمات معاً**
```bash
bash start-all.sh
```

**خيار 2: تشغيل كل خدمة بشكل منفصل**
```bash
# Terminal 1
bash start-agent.sh

# Terminal 2
bash start-dashboard.sh
```

### 4. الوصول إلى الواجهة

افتح المتصفح:
```
http://YOUR_IP:8000
```

## 🎯 الميزات

### ✅ إدارة Stacks
- عرض حالة جميع stacks
- إعادة تشغيل stacks
- تحديث Docker images

### ✅ إدارة Sites
- عرض جميع sites
- إعادة تشغيل sites
- تشغيل migrations
- إنشاء نسخ احتياطية

### ✅ النسخ الاحتياطية
- إنشاء نسخ احتياطية يدوية
- تحميل النسخ الاحتياطية
- جدولة نسخ احتياطية تلقائية

### ✅ المجدول
- نسخ احتياطية يومية/أسبوعية/شهرية
- تحديد الوقت
- إدارة المهام المجدولة

## 🔧 التخصيص

### إضافة Stack جديد

عدّل `config.yaml`:
```yaml
stacks:
  my-stack:
    path: /path/to/stack
    type: fm
```

ثم أعد تشغيل الخدمات.

### تغيير كلمة المرور

عدّل `config.yaml`:
```yaml
dashboard:
  admin_username: admin
  admin_password: new-password
```

ثم أعد تشغيل Dashboard.

## 📁 هيكل المشروع

```
dash/
├── agent/              # خدمة Agent
├── dashboard/          # خدمة Dashboard
├── config.yaml         # ملف التكوين
├── install.sh          # سكريبت التثبيت
├── start-all.sh        # تشغيل جميع الخدمات
├── start-agent.sh      # تشغيل Agent
└── start-dashboard.sh  # تشغيل Dashboard
```

## 🔐 الأمان

### ⚠️ مهم للإنتاج

Dashboard حالياً يستمع على `0.0.0.0:8000` (مكشوف على الشبكة).

**للإنتاج:**

1. **استخدم Nginx كـ reverse proxy**
```bash
sudo cp nginx/fm-dashboard.conf /etc/nginx/sites-available/
sudo ln -s /etc/nginx/sites-available/fm-dashboard.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

2. **فعّل SSL**
```bash
sudo certbot --nginx -d dashboard.yourdomain.com
```

3. **غيّر Dashboard ليستمع على localhost فقط**
```yaml
dashboard:
  listen: 127.0.0.1  # ✅ آمن
```

## 🛠️ استكشاف الأخطاء

### الخدمات لا تعمل؟

```bash
# تحقق من logs
cd /opt/dash
source venv/bin/activate
python agent/main.py      # سترى الأخطاء إن وجدت
python dashboard/main.py  # سترى الأخطاء إن وجدت
```

### لا يمكن الوصول للواجهة؟

```bash
# تحقق من Firewall
sudo ufw status
sudo ufw allow 8000

# تحقق من IP
hostname -I
```

### Stacks لم يتم اكتشافها؟

عدّل `config.yaml` يدوياً:
```yaml
stacks:
  production:
    path: /home/baron/frappe-bench
    type: fm
```

## 📚 وثائق إضافية

- `README.md` - وثائق كاملة بالإنجليزية
- `QUICKSTART.md` - دليل سريع
- `DEPLOYMENT.md` - دليل النشر للإنتاج
- `SECURITY.md` - إرشادات الأمان
- `ARCHITECTURE.md` - معمارية النظام

## 🔄 التحديث

```bash
cd /opt/dash
git pull
bash install.sh  # سيحدث الإعدادات
```

## 📞 الدعم

للمشاكل والاستفسارات:
1. راجع قسم استكشاف الأخطاء
2. تحقق من logs
3. راجع الوثائق

## 🎉 نصائح

### تثبيت كـ systemd services (اختياري)

للتشغيل التلقائي عند بدء النظام:

```bash
sudo bash scripts/install-services.sh
```

ثم:
```bash
sudo systemctl status fm-agent fm-dashboard
sudo journalctl -u fm-agent -f
```

### جدولة نسخ احتياطية

1. افتح الواجهة
2. اذهب إلى "Scheduler"
3. اختر Stack و Site
4. حدد الجدول الزمني
5. اضغط "Add Schedule"

### تحميل نسخة احتياطية

1. اذهب إلى Stack
2. اختر Site
3. اضغط "View Backups"
4. اضغط "Download" للنسخة المطلوبة

---

**الترخيص:** MIT  
**الإصدار:** 1.0.0  
**تم البناء بـ:** FastAPI, Python, HTMX, Tailwind CSS

